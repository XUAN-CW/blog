version: '3.8'

services:
  nginx:
    image: nginx:1.25.3
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - nexus3
    networks:
      - nexus-network

  nexus3:
    image: sonatype/nexus3:3.62.0
    volumes:
      - type: volume
        source: nexus-data
        target: /nexus-data
    networks:
      - nexus-network

networks:
  nexus-network:
    driver: bridge

volumes:
  nexus-data:
    name: nexus-data
